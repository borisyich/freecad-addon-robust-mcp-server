interface:
  display_name: "FreeCAD Engineering"
  short_description: "FreeCAD part modeling and model editing workflows"
  default_prompt: "Use $freecad-engineering for this FreeCAD engineering task."
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "freecad"
      description: "Local FreeCAD Robust MCP server"
